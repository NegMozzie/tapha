{% extends 'BlogBundle:Frontend/Layout:main.html.twig' %}

{% block content %}
  <div class=" margin--t">
    <div class="row">
      <div class="col-md-3 col-lg-3">
        <div id="mz_sidebar" class="semi-transparent-round">
                  <div id="mz_sidebar_title" class="semi-transparent-round">
                      Evenements de la semaine
                  </div> 
                  <div class="semi-transparent-round" margin-top:"5px;">
                    {% render(controller('BlogBundle:Frontend/Sidebar:blogList')) %}

                  </div>  
              </div>
      </div>
      <div class="container">
		{% for item in season.getEvents() %}
    		<div class="col-md-3" style="margin-top:5px;">
          <h1>h1 Bootstrap heading (36px)</h1>
    			<a class="black" href="{{ path('ed_frontend_blog_by_eventname', { 'eventName': item.getName(), 'type': "championnat"  }) }}">
		            <div>
		            	<img class="" src="{% path item.excerptPhoto, 'big' %}">
		            </div>
		            {{item.name}}
        		</a>
    		</div>
		{% endfor %}
       
      </div>
  </div>
{% endblock content %}